"use client";
import {useEffect,useMemo,useState} from "react";
const NAV=["Home","Draft","Guide","Coach"],POS=["ALL","QB","RB","WR","TE"],TABS=["Home","Start/Sit","Waivers","Trades","Lineup","Watch","Analysts","League"],SLOT={0:"QB",2:"RB",4:"WR",6:"TE",16:"DST",17:"K",20:"BE",21:"IR",23:"FLEX"},KICKOFF=new Date("2026-09-09T20:20:00-04:00");
const fmt=(v,d=1,s="")=>v==null||Number.isNaN(Number(v))?"—":`${Number(v).toFixed(d)}${s}`;
function clock(){const x=Math.max(0,Math.floor((KICKOFF-Date.now())/1000));if(!x)return"LIVE";const p=v=>String(v).padStart(2,"0");return`${p(Math.floor(x/86400))}D ${p(Math.floor(x%86400/3600))}H ${p(Math.floor(x%3600/60))}M ${p(x%60)}S`}
function Icon({n}){const d={Home:"M3 11.5 12 4l9 7.5V21h-6v-6H9v6H3z",Draft:"M5 5h14v14H5z M8 9h8 M8 13h8 M8 17h5",Guide:"M5 4h11a3 3 0 0 1 3 3v13H8a3 3 0 0 0-3 1z M8 8h7 M8 12h7",Coach:"m12 3 1.5 4.5L18 9l-4.5 1.5L12 15l-1.5-4.5L6 9l4.5-1.5z",Players:"M4 20a5 5 0 0 1 10 0 M14 20a4 4 0 0 1 7 0 M9 5a3 3 0 1 1 0 6 3 3 0 0 1 0-6z M17 7a2.5 2.5 0 1 1 0 5 2.5 2.5 0 0 1 0-5z"};return <svg viewBox="0 0 24 24"><path d={d[n]||d.Home}/></svg>}
function Pills({items,value,set}){return <div className="pills">{items.map(x=><button key={x} className={x===value?"active":""} onClick={()=>set(x)}>{x}</button>)}</div>}
function Head({k,t,c}){return <div className="head"><span>{k}</span><h1>{t}</h1><p>{c}</p></div>}
function Notice({children,error}){return <div className={`notice ${error?"error":""}`}>{children}</div>}
function PlayerSelect({label,players,value,set}){return <label className="field"><span>{label}</span><select value={value} onChange={e=>set(e.target.value)}>{players.map(p=><option key={p.name} value={p.name}>{p.name} — {p.pos} {p.team}</option>)}</select></label>}
function RankingList({players}){return <div className="rank-list">{players.map(p=><div key={p.name}><em>#{p.overallRank}</em><i>{p.pos}</i><section><b>{p.name}</b><small>{p.team} · Bye {p.bye??"—"}</small></section><strong>ADP {fmt(p.adp)}</strong></div>)}</div>}
export default function ShivaShell(){const[page,setPage]=useState("Home"),[players,setPlayers]=useState([]),[err,setErr]=useState(""),[splash,setSplash]=useState(true),[tick,setTick]=useState(clock());useEffect(()=>{const t=setTimeout(()=>setSplash(false),1450);return()=>clearTimeout(t)},[]);useEffect(()=>{const t=setInterval(()=>setTick(clock()),1000);return()=>clearInterval(t)},[]);useEffect(()=>{fetch("/api/players").then(r=>r.ok?r.json():Promise.reject()).then(d=>setPlayers(d.players||[])).catch(()=>setErr("Player rankings unavailable."))},[]);return <main className="shell">{splash&&<div className="splash"><img src="/shiva-trophy.png" alt=""/><b>SHIVA</b></div>}<header><button className="brand" onClick={()=>setPage("Home")}><img src="/shiva-trophy.png" alt=""/><div><b>SHIVA</b><span>Fantasy Football Intelligence</span></div></button>{page==="Home"&&<div className="kick"><span>NFL KICKOFF</span><b>{tick}</b></div>}</header><div className="view">{err&&<Notice error>{err}</Notice>}{page==="Home"&&<Home go={setPage}/>} {page==="Draft"&&<Draft players={players}/>} {page==="Guide"&&<Guide players={players}/>} {page==="Players"&&<Players players={players}/>} {page==="Coach"&&<Coach players={players}/>}</div><nav>{NAV.map(n=><button key={n} className={page===n?"active":""} onClick={()=>setPage(n)}><Icon n={n}/><span>{n}</span></button>)}</nav></main>}
function Home({go}){const[edge,setEdge]=useState(null),[mode,setMode]=useState(""),[news,setNews]=useState([]),[error,setError]=useState("");useEffect(()=>{Promise.allSettled([fetch("/api/edge").then(r=>r.json()),fetch("/api/espn?action=news").then(r=>r.json())]).then(([e,n])=>{if(e.status==="fulfilled"&&!e.value.error)setEdge(e.value);else setError("Shiva Edge is temporarily unavailable.");if(n.status==="fulfilled")setNews((n.value.articles||[]).slice(0,6))})},[]);const floor=edge?.floor||[],ceil=edge?.ceiling||[],list=mode==="floor"?floor:ceil,metric=mode==="floor"?"rate15":"boom25";return <section><div className="home-grid">{[["Draft","Run a live mock"],["Coach","Start / sit & waivers"],["Guide","Rankings & strategy"],["Players","Search the board"]].map(([n,c])=><button key={n} onClick={()=>go(n)}><span><Icon n={n}/></span><b>{n}</b><small>{c}</small><em>›</em></button>)}</div><div className="title"><span>THE SHIVA EDGE</span><h2>Raise the floor. Keep the ceiling.</h2></div><div className="edge-grid"><button className={mode==="floor"?"active":""} onClick={()=>setMode(mode==="floor"?"":"floor")}><span>RAISE THE FLOOR</span><h3>Reliable 15+ weeks</h3>{floor[0]?<><b>{floor[0].name}</b><strong>{fmt(floor[0].rate15,0,"%")}</strong><small>{floor[0].pos} · {floor[0].team}</small></>:<small>Loading verified history…</small>}<em>Floor rankings →</em></button><button className={mode==="ceiling"?"active":""} onClick={()=>setMode(mode==="ceiling"?"":"ceiling")}><span>KEEP THE CEILING</span><h3>Week-winning 25+</h3>{ceil[0]?<><b>{ceil[0].name}</b><strong>{fmt(ceil[0].boom25,0,"%")}</strong><small>{ceil[0].pos} · {ceil[0].team}</small></>:<small>Loading verified history…</small>}<em>Ceiling rankings →</em></button></div>{error&&<Notice error>{error}</Notice>}{mode&&list.length>0&&<div className="edge-panel"><div><span>{mode==="floor"?"FLOOR RANKINGS":"CEILING RANKINGS"}</span><button onClick={()=>setMode("")}>Close</button></div>{list.map((p,i)=><section key={p.name}><em>#{i+1}</em><div><b>{p.name}</b><small>{p.pos} · {p.team} · {fmt(p.ppg)} PPG</small></div><strong>{fmt(p[metric],0,"%")}</strong></section>)}</div>}<div className="title blast"><span>SHIVA BLAST</span><h2>What matters right now.</h2></div>{!news.length&&<Notice>Loading current ESPN context…</Notice>}<div className="news">{news.map((a,i)=><a key={i} href={a.url||"#"} target="_blank" rel="noreferrer">{a.image?<img src={a.image} alt=""/>:<i/>}<div><span>ESPN</span><b>{a.headline}</b><p>{a.description}</p><em>Read story →</em></div></a>)}</div></section>}
function teamForPick(pick,teams){const r=Math.floor((pick-1)/teams)+1,w=(pick-1)%teams+1;return r%2?w:teams-w+1}
function Draft({players}){const teams=10,[slot,setSlot]=useState(1),[started,setStarted]=useState(false),[drafted,setDrafted]=useState([]),[pos,setPos]=useState("ALL");const taken=useMemo(()=>new Set(drafted.map(d=>d.player.name)),[drafted]),next=drafted.length+1,onClock=started&&teamForPick(next,teams)===slot,board=players.filter(p=>!taken.has(p.name)&&(pos==="ALL"||p.pos===pos)).slice(0,36);const sim=seed=>{const copy=[...seed];while(copy.length<150){const pick=copy.length+1;if(teamForPick(pick,teams)===slot)break;const used=new Set(copy.map(d=>d.player.name)),p=players.find(x=>!used.has(x.name));if(!p)break;copy.push({pick,team:teamForPick(pick,teams),player:p})}return copy};if(!started)return <section><Head k="DRAFT ROOM" t="Mock Draft" c="A clean, phone-first snake draft room built around your decisions."/><div className="hero"><span>MOCK DRAFT</span><h2>Start the room.</h2><p>Pick your slot. Shiva simulates the managers between your turns without cluttering the screen.</p><label className="field"><span>Your draft position</span><select value={slot} onChange={e=>setSlot(Number(e.target.value))}>{Array.from({length:10},(_,i)=>i+1).map(n=><option key={n}>Pick #{n}</option>)}</select></label><button className="primary" onClick={()=>{setDrafted(sim([]));setStarted(true)}}>Start Mock Draft</button></div></section>;return <section><Head k="DRAFT ROOM" t="Mock Draft" c="The board stays large, readable and focused on your next decision."/><div className="stats"><div><span>PICK</span><b>{next}</b></div><div><span>ROUND</span><b>{Math.floor((next-1)/teams)+1}</b></div><div><span>YOUR SLOT</span><b>#{slot}</b></div></div><div className={`clock ${onClock?"live":""}`}>{onClock?"YOU'RE ON THE CLOCK":"Simulating between your picks"}</div><Pills items={POS} value={pos} set={setPos}/><div className="board">{board.map(p=><button key={p.name} disabled={!onClock} onClick={()=>onClock&&setDrafted(sim([...drafted,{pick:next,team:slot,player:p}]))}><em>#{p.overallRank}</em><div><b>{p.name}</b><small>{p.pos} · {p.team} · Bye {p.bye??"—"}</small></div><strong>ADP {fmt(p.adp)}</strong></button>)}</div><button className="secondary" onClick={()=>{setStarted(false);setDrafted([])}}>Reset Draft</button></section>}
function Guide({players}){const[tab,setTab]=useState("Rankings"),[pos,setPos]=useState("ALL");return <section><Head k="2026 DRAFT GUIDE" t="Build a better draft." c="Current rankings, roster construction and full-PPR decision rules."/><Pills items={["Rankings","Strategy","Targets","Rules"]} value={tab} set={setTab}/>{tab==="Rankings"&&<><Pills items={POS} value={pos} set={setPos}/><RankingList players={players.filter(p=>pos==="ALL"||p.pos===pos).slice(0,60)}/></>}{tab==="Strategy"&&<Cards items={[["Rounds 1–3","Prioritize elite workload and target volume. Pay for difference-makers, not replaceable production."],["Rounds 4–7","Attack falling tiers before positional runs force bad value."],["Rounds 8–11","Build contingency value: ambiguous backfields, ascending receivers and high-route tight ends."],["Late rounds","Chase paths to relevance. Bench spots should create leverage, not comfort."]]}/>} {tab==="Targets"&&<Cards items={players.slice(0,10).map(p=>[p.name,`${p.pos} · ${p.team} · Overall #${p.overallRank} · ADP ${fmt(p.adp)}`])}/>} {tab==="Rules"&&<div className="article"><h2>Shiva rules</h2><p><b>Raise the floor.</b> Favor players who repeatedly create startable weeks.</p><p><b>Keep the ceiling.</b> Preserve enough explosive outcomes to win high-scoring matchups.</p><p><b>Draft the room.</b> What managers between your picks need changes what is actually scarce.</p><p><b>Separate facts from projections.</b> Historical evidence and current rankings are different inputs.</p></div>}</section>}
function Cards({items}){return <div className="cards">{items.map(([t,c])=><div key={t}><span>SHIVA GUIDE</span><b>{t}</b><p>{c}</p></div>)}</div>}
function Players({players}){const[pos,setPos]=useState("ALL"),[q,setQ]=useState("");const list=players.filter(p=>(pos==="ALL"||p.pos===pos)&&(!q||`${p.name} ${p.team}`.toLowerCase().includes(q.toLowerCase()))).slice(0,100);return <section><Head k="PLAYER BOARD" t="Players" c="Search the current Shiva ranking board without squeezing it into tiny cards."/><label className="field"><span>Search players</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Name or team"/></label><Pills items={POS} value={pos} set={setPos}/><RankingList players={list}/></section>}
function useEvidence(names){const key=names.join("|");const[state,setState]=useState({loading:false,data:null,error:""});useEffect(()=>{if(!names.length||names.some(n=>!n)){setState({loading:false,data:null,error:""});return}let alive=true;setState({loading:true,data:null,error:""});fetch(`/api/coach?players=${encodeURIComponent(key)}`).then(r=>r.ok?r.json():Promise.reject()).then(data=>alive&&setState({loading:false,data,error:""})).catch(()=>alive&&setState({loading:false,data:null,error:"Evidence lookup failed."}));return()=>{alive=false}},[key]);return state}
function Coach({players}){const[tab,setTab]=useState("Home"),[league,setLeague]=useState(null);return <section><Head k="SHIVA COACH" t="Make the decision. See the why." c="Start/sit, waivers, trades, lineup checks and ESPN league context."/><Pills items={TABS} value={tab} set={setTab}/><div className="coach"><span>SHIVA SAYS</span><h2>{tab==="Home"?"Your command center":tab}</h2><p>{tab==="Home"?"Fast decisions, clear evidence, no fake confidence numbers.":"Current rankings and verified historical scoring stay separate so the recommendation is explainable."}</p></div>{tab==="Home"&&<Cards items={[["Thursday FLEX protection","Keep early-week players out of FLEX so Sunday replacement options stay open."],["Read the managers between picks","Use roster construction around the turn instead of blindly following ADP."],["Raise the floor","Repeatable 15+ point weeks keep you alive in bad matchup environments."],[league?league.name:"ESPN not connected",league?`${league.teams.length} teams · scoring period ${league.scoringPeriod??"—"}`:"Connect your ESPN league for roster-aware context."]]}/>} {tab==="Start/Sit"&&<StartSit players={players}/>} {tab==="Waivers"&&<Waivers players={players} league={league}/>} {tab==="Trades"&&<Trades players={players}/>} {tab==="Lineup"&&<Lineup players={players}/>} {tab==="Watch"&&<Watch players={players}/>} {tab==="Analysts"&&<Analysts/>} {tab==="League"&&<League league={league} setLeague={setLeague}/>}</section>}
function StartSit({players}){const[a,setA]=useState(""),[b,setB]=useState("");useEffect(()=>{if(players.length){setA(v=>v||players[0].name);setB(v=>v||players[1]?.name||players[0].name)}},[players]);const r=useEvidence(a&&b&&a!==b?[a,b]:[]);return <div><div className="two"><PlayerSelect label="Player A" players={players} value={a} set={setA}/><PlayerSelect label="Player B" players={players} value={b} set={setB}/></div>{r.loading&&<Notice>Loading verified scoring evidence…</Notice>}{r.error&&<Notice error>{r.error}</Notice>}{r.data&&<><div className="call"><span>SHIVA SAYS</span><h3>Start {r.data.recommendation}</h3><p>Based on weekly floor, average scoring, ceiling, consistency and current rank context.</p></div><div className="metrics">{r.data.evidence.map(e=><div className={e.name===r.data.recommendation?"winner":""} key={e.name}><b>{e.name}</b><small>{e.pos} · {e.team}</small><section><span><b>{fmt(e.floor)}</b>Floor</span><span><b>{fmt(e.ppg)}</b>PPG</span><span><b>{fmt(e.ceiling)}</b>Ceiling</span><span><b>{fmt(e.rate15,0,"%")}</b>15+ Weeks</span></section></div>)}</div></>}</div>}
function Waivers({players,league}){const roster=new Set((league?.teams||[]).flatMap(t=>t.roster||[]).map(r=>r.player));return <div><p className="copy">{league?"Rostered players in your connected league are excluded.":"Connect ESPN to make this roster-aware. Until then, this is current rank context."}</p><RankingList players={players.filter(p=>!roster.has(p.name)).slice(0,35)}/></div>}
function Trades({players}){const[a,setA]=useState(""),[b,setB]=useState(""),[c,setC]=useState(""),[d,setD]=useState("");useEffect(()=>{if(players.length){setA(v=>v||players[0]?.name);setB(v=>v||players[1]?.name);setC(v=>v||players[2]?.name);setD(v=>v||players[3]?.name)}},[players]);const r=useEvidence([a,b,c,d].filter(Boolean));let give=0,receive=0;if(r.data){const m=new Map(r.data.evidence.map(e=>[e.name,e.evidenceScore||0]));give=(m.get(a)||0)+(m.get(b)||0);receive=(m.get(c)||0)+(m.get(d)||0)}return <div><div className="two"><div><h3>You give</h3><PlayerSelect label="Player 1" players={players} value={a} set={setA}/><PlayerSelect label="Player 2" players={players} value={b} set={setB}/></div><div><h3>You receive</h3><PlayerSelect label="Player 1" players={players} value={c} set={setC}/><PlayerSelect label="Player 2" players={players} value={d} set={setD}/></div></div>{r.data&&<div className="call"><span>SHIVA SAYS</span><h3>{receive>=give?"Receive side has the stronger evidence profile":"Keep your current side"}</h3><p>Package comparison uses the same historical floor-first evidence model and current rank context.</p></div>}</div>}
function Lineup({players}){const[name,setName]=useState(""),[game,setGame]=useState(null),[err,setErr]=useState("");useEffect(()=>{if(players.length)setName(v=>v||players[0].name)},[players]);const run=async()=>{setErr("");try{const p=players.find(x=>x.name===name),r=await fetch("/api/espn?action=scoreboard"),d=await r.json();if(!r.ok)throw 0;let found=null;for(const ev of d.events||[]){if(ev.competitions?.[0]?.competitors?.some(c=>c.team?.abbreviation===p?.team)){found={day:new Date(ev.date).toLocaleDateString("en-US",{weekday:"long"}),event:ev.shortName||ev.name};break}}setGame(found||{day:null,event:"No current scoreboard game found."})}catch{setErr("Schedule unavailable.")}};return <div><PlayerSelect label="Player currently in FLEX" players={players} value={name} set={setName}/><button className="primary" onClick={run}>Check lineup edge</button>{err&&<Notice error>{err}</Notice>}{game&&<div className={`call ${game.day==="Thursday"?"danger":""}`}><span>LINEUP CHECK</span><h3>{game.day==="Thursday"?`Move ${name} out of FLEX`:"No Thursday FLEX trap detected"}</h3><p>{game.day?`${game.event} is scheduled for ${game.day}.`:game.event}</p></div>}</div>}
function Watch({players}){const[name,setName]=useState(""),[articles,setArticles]=useState([]);useEffect(()=>{if(players.length)setName(v=>v||players[0].name)},[players]);return <div><PlayerSelect label="Watch player" players={players} value={name} set={setName}/><button className="primary" onClick={async()=>{const r=await fetch(`/api/espn?action=news&player=${encodeURIComponent(name)}`),d=await r.json();setArticles(d.articles||[])}}>Check ESPN context</button><div className="watch">{articles.map((a,i)=><a key={i} href={a.url||"#"} target="_blank" rel="noreferrer"><span>ESPN</span><b>{a.headline}</b><p>{a.description}</p></a>)}</div></div>}
function Analysts(){const[report,setReport]=useState("");return <div><p className="copy">Upload a weekly ranking snapshot. Shiva validates structure before any historical grading.</p><label className="upload"><span>Upload rankings CSV</span><input type="file" accept=".csv,text/csv" onChange={e=>{const f=e.target.files?.[0];if(!f)return;f.text().then(t=>{const[h,...rows]=t.trim().split(/\r?\n/),cols=h.split(",").map(x=>x.trim().toLowerCase()),missing=["analyst","player","rank","season","week"].filter(x=>!cols.includes(x));setReport(missing.length?`Missing required columns: ${missing.join(", ")}`:`Validated ${rows.length} ranking rows.`)})}}/></label>{report&&<Notice>{report}</Notice>}</div>}
function League({league,setLeague}){const[id,setId]=useState(""),[season,setSeason]=useState(2026),[swid,setSwid]=useState(""),[s2,setS2]=useState(""),[status,setStatus]=useState("");const connect=async()=>{setStatus("Connecting…");try{const r=await fetch("/api/espn",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({leagueId:id,season,swid,espnS2:s2})}),d=await r.json();if(!r.ok)throw new Error(d.error);setLeague(d.league);setStatus("")}catch(e){setStatus(e.message||"ESPN connection failed.")}};if(league)return <div><div className="call"><span>ESPN LEAGUE CONNECTED</span><h3>{league.name}</h3><p>{league.season} · {league.teams.length} teams · scoring period {league.scoringPeriod??"—"}</p></div><div className="teams">{league.teams.map(t=><div key={t.id}><b>{t.name}</b><span>{t.wins??"—"}-{t.losses??"—"}</span><small>{(t.roster||[]).filter(r=>SLOT[r.lineupSlotId]!=="BE").slice(0,8).map(r=>r.player).filter(Boolean).join(" · ")}</small></div>)}</div><button className="secondary" onClick={()=>setLeague(null)}>Disconnect league</button></div>;return <div><div className="two"><label className="field"><span>ESPN League ID</span><input value={id} onChange={e=>setId(e.target.value)}/></label><label className="field"><span>Season</span><input value={season} onChange={e=>setSeason(Number(e.target.value))}/></label></div><details><summary>Private league credentials</summary><label className="field"><span>SWID</span><input type="password" value={swid} onChange={e=>setSwid(e.target.value)}/></label><label className="field"><span>espn_s2</span><input type="password" value={s2} onChange={e=>setS2(e.target.value)}/></label></details><button className="primary" onClick={connect}>Connect ESPN league</button>{status&&<Notice error={status!=="Connecting…"}>{status}</Notice>}</div>}
